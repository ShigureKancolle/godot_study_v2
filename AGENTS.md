# 项目协作约定

## 首版之前的最高优先级

- 本项目当前最高优先级是跑通“登录 → 进入游戏 → 完成一局游戏 → 游戏结束 → 重开一局”的完整流程。所有需求和技术决策先判断是否服务于这一目标。
- 在项目具备发布第一个版本的条件之前，不做大范围迭代、整体重构、框架替换或推倒重来；仅在现有框架上做满足当下需求的最小修改。
- 面对需求，优先使用基本、直接、易理解的编程方式，做到短期可维护即可。不要为了假设中的未来需求提前设计通用框架、扩展层或优化方案。
- 调试功能服务于当前开发和流程验证，优先沿用现有通信框架增加协议与处理，不主动扩展为独立调试系统或通用网络框架。
- 当用户开始考虑过远的扩展、优化或重构时，Codex 应温和提醒先完成当前需求，并说明最小可行的局部处理方式，不顺势扩大项目范围。

## 保持范围与推进节奏

- 每次只推进一个明确需求，先说清本次要达到的行为和完成条件，完成后再处理下一项。
- 新想法、非阻塞问题和未来优化先记录为待办；已有待办或旧计划中的条目不自动成为首版的必做事项。
- 判断是否需要结构调整时，先回答“现有结构是否实际阻碍当前需求或完整流程”；代码不够通用、不够优雅、未来可能不够快，都不能单独成为重构理由。
- 确有阻塞时，先定位具体问题，再做必要的局部修复；若局部处理不可行，先说明证据和最小调整范围，不擅自升级为整体重构。
- 以实际行为是否达到约定的完成条件判断进度，允许保留已知但不阻塞流程的局限；不在需求完成后主动追加美化、抽象或架构升级。
- 用户以精进编程能力为目标。对于明确用于练习的功能，优先让用户提出规则、判断和方案，Codex 提供检查与逐步提示；用户明确要求直接实现时按授权执行。
- 检查与验收保持适度：常规修改只做静态检查及相关项目启动阻塞检查；用户明确要求阶段验证时，再按授权范围执行功能验收和联调，遵守下方记录要求。

## 代码与协作规范

- 所有 Python 源文件必须保留首行 `# coding=utf-8`，新增 Python 文件也必须包含该声明。
- 代码注释、文档字符串和模块职责说明统一使用中文。
- 类名、函数名、协议字段名、错误码等机器契约保持原有英文，不为满足注释语言要求而翻译标识符。
- Protobuf 生成物禁止手工修改；协议变更后使用项目同步脚本重新生成。
- 验收和联调由 Codex 负责编写用例、准备必要测试脚本、执行并记录结果；包括纯服务端测试、Godot 客户端验收和双客户端联调，不默认交给用户手测。
- 验收记录区分通过、失败、未执行和环境受限，注明命令、环境及证据；无法执行的项目说明具体原因，不得标为通过。
- 用户明确暂缓的 BUG 只记录现象、影响和后续验收条件，不擅自修复，也不作为继续当前开发的前置阻碍。
- 客户端跨模块业务事件统一经 `SignalMgr` 定义、发出和订阅；组件内部及父子节点间的生命周期通知保留局部信号。局部信号不得用于判断权威战斗结果，具体边界见 [模块职责中的信号约定](docs/MODULE_RESPONSIBILITIES.md#客户端信号约定)。
